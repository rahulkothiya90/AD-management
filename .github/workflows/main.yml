name: AD Management

on:
  push:
    branches: [ "main" ]

jobs:
  deploy-ad:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4

      - name: Run AD script
        shell: powershell
        working-directory: scripts
        run: .\apply.ps1
